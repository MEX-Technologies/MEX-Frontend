{
  "name": "mex-frontend",
  "version": "1.0.0",
  "description": "MEX - Cross-border payment platform using USDC and USDT",
  "homepage": "https://MEX-Technologies.github.io/MEX-Frontend",
  "private": true,
  "dependencies": {
    "@carbon/icons-react": "^11.5.0",
    "@carbon/react": "^1.40.0",
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    "react-scripts": "5.0.1"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test",
    "eject": "react-scripts eject",
    "predeploy": "npm run build",
    "deploy": "gh-pages -d build"
  },
  "eslintConfig": {
    "extends": [
      "react-app"
    ]
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  },
  "devDependencies": {
    "gh-pages": "^6.3.0",
    "sass": "^1.94.2"
  }
}
